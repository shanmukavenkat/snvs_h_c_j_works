// the scope function
function greet(){
    var message = "Hello";
    console.log(message);
}

greet()
console.log(message);

///here the scope can be accessed only in the
// inside the function can't be accessed outside the function
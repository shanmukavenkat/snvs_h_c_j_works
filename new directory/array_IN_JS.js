let myArray = [5,343.9,"snvskomal",4684];
console.log(myArray);
//we can access the elements of an array using index
//here indexing starts from 0
console.log(myArray[0]);
console.log(myArray[3]);
//here we can modify the elements of the array
//here the array is mutable which means we can change the values of the array
myArray[0]=10;
console.log(myArray);
// we can check the length the array using length property
console.log(myArray.length);